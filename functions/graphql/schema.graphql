# Construct a schema, using GraphQL schema language
type Query {
  getArticles: [Article]
  hello: String
}

type Article {
  id: ID
  title: String
  summary: String
  sourceREF: String
  source: String
  mongodb_id: String
  logo: String
  link: String
}
